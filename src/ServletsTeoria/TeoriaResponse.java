package ServletsTeoria;

public class TeoriaResponse {
    /*

    Response (respuesta) es posiblemente el objeto más utilizado de todos, ya que sirve para presentar en la pantalla
    del navegador del cliente el resultado de cualquier código que hayamos escrito.

        Para generar una pagina HTML con un servlet debemos seguir dos pasos:

         1. Indicar que el contenido que se va a enviar es HTML (Mediante el metodo setContentType() como se muestra abajo
        Esta linea es una cabecera de respuesta, que veremos mas adelante como utilizar. Hay que ponerla antes de obtener
        el Writer.

        2.Escribir en el flujo de salida el texto necesario para generar la pagina HTML. La linea que genera el DOCTYPE no es
        necesaria, aunque si muy recomendada para que se sepa que version de HTML se esta empleando.


    response.setContentType("text/html;charset=UTF-8");

    PrintWriter out = response.getWriter(); // Metodo con el que podemos enviar datos al cliente.










     */
}
